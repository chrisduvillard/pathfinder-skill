2026-08-10 P0.1 complete — replaced GNU-only validator fixture edits with portable rewrites, added a negative portability fixture, and verified test-validators, portability, and full check-all pass on macOS; no contradictions.
2026-08-10 P0.2 complete — removed the duplicate artifact-eval invocation and verified check-all passes with one artifact-eval section; no contradictions.
2026-08-10 decisions ratified — user approved D-01 through D-08 with the plan's recommended choices.
2026-08-10 P0.3 implemented locally — added required Ubuntu/macOS/Windows preflight matrix and local portability/manifest checks pass; hosted three-OS run remains pending until publication.
2026-08-10 P0.4 complete — corrected Claude/Codex invocation distinctions and added official Codex Goal enablement/lifecycle guidance; manifest, consistency, search, and whitespace checks pass.
2026-08-10 P1.1 complete — ratified the autonomy controller contract, v1 scope, trust boundaries, degradation behavior, and D-01 through D-08; reviewed against F-06 through F-16 and git diff --check passes.
2026-08-10 P1.2 complete — added versioned charter/doctrine/roadmap schemas with separate intent_clarity and per-item execution_eligibility; negative duplicate-key, enum, field, and stale-version tests pass.
2026-08-10 P1.3 complete — added canonical candidate, verification, Goal Binding, runtime, run-log, summary, authorization, state, and event schemas; all contract schemas parse and validate with RFC 3339 format checks.
2026-08-10 P1.4 complete — removed ordinary-run auto-escalation, self-merge, autonomous doctrine mutation, ambiguous manual approval, parallel v1 execution, and unbounded opportunity derivation; full legacy checks and schema tests pass.
2026-08-10 P2.1 complete — added read-only doctor capability reporting with unknown enforcement failing closed; capability tests and doctor JSON pass.
2026-08-10 P2.2 complete — added closed mission transitions, atomic state, append-only events, portable lease lock, interrupted-write recovery, and concurrency/idempotency tests.
2026-08-10 P2.3 complete — added Git/non-Git probing, dirty-tree block, exact base binding, hook/credential-neutralized Git, safe worktrees, and conservative cleanup tests.
2026-08-10 P2.4 complete — added structured-argv execution policy with runtime eligibility, secret/credential/destructive-action denies, timeout, hashing, and redaction; execution tests pass.
2026-08-10 P2.5 complete — added Codex, Claude, and generic Goal adapters; unfinished Codex Goals are reused or protected and unavailable native APIs produce manual fallback.
2026-08-10 P2.6 complete — added fixture-backed, credential-separated, idempotent GitHub publication with bounded checks and explicit failure states; no merge operation exists.
2026-08-10 P2.7 complete — added one-Goal orchestration and mission status CLI; transition-level crash/resume tests reach awaiting-review with at most one callback invocation per checkpoint.
2026-08-10 P3.1 complete — reduced the main skill to a thin router/trust boundary and moved route behavior into 13 required route modules with logical parity validation.
2026-08-10 P3.2 complete — added a prompt-to-Goal fast path that skips the creator interview when the contract is clear and keeps Goal activation separate from autonomous authority.
2026-08-10 P3.3 complete — routed explicit autonomous requests through the controller launcher with fail-closed capability checks, one Goal, and awaiting-review publication.
2026-08-10 P3.4 complete — added a conservative repository/scope/commit/config discovery cache with atomic storage and invalidation tests.
2026-08-10 P4.1 complete — replaced brace-shaped artifact checks with duplicate-safe JSON Schema validation, exact `/goal` parsing, laundering negatives, and cross-artifact references.
2026-08-10 P4.2 partial — added full state-transition, resume, dirty-tree, hooks, symlink, credential, command-injection, and publication-idempotency tests; command-boundary crash journaling and exhaustive provenance/forge matrices remain open.
2026-08-10 P4.3 complete with limitation — added six schema-validated synthetic controller replays; replays captured from real host/model Pathfinder runs remain open.
2026-08-10 P4.4 complete as advisory infrastructure — added five bounded live-model cases and an external adapter contract; no paid/live model was invoked locally.
2026-08-10 P5.1 complete — added validated intent/mission migrations with backups, unknown-version refusal, and rollback tests without granting authorization or inventing clarity.
2026-08-10 P5.2 implemented locally — bumped to v3.0.0, pinned stable marketplaces to `v3.0.0`, documented `main` as edge, and added exact-archive release smoke; no tag or release was created.
2026-08-10 P5.3 complete — added compatibility, operator, threat-model, examples, coverage, retention, degradation, and guarantee-boundary documentation.
2026-08-10 package-smoke correction — final validation exposed a relative-interpreter false-positive after changing directories; the smoke runner now resolves the repository absolutely and fails immediately when any packaged check or outside-CWD launch fails.
2026-08-10 final local verification — full preflight, 74 controller tests, 16 artifact cases, six controller replays, unpacked package smoke, plugin validation, skill validation, and whitespace/conflict checks pass on macOS; hosted Linux/macOS/Windows jobs, live-model cases, tag creation, and release remain pending external publication.
2026-08-10 hosted CI correction — the first macOS job exposed a split interpreter (`python` received dependencies while Apple `python3` ran checks); the matrix now binds controller and eval checks to its installed Python on every OS.
2026-08-10 Windows CI correction — hosted tests exposed CRLF-sensitive intent metadata and loss of effective `core.autocrlf` in the neutralized Git environment; migrations now accept CRLF and Git preserves only the resolved line-ending policy while hooks/credentials stay disabled.
2026-08-10 host dogfood correction — isolated Codex install and Claude load/status passed; live three-file prompt-to-Goal runs exposed 163-second placeholder churn, an unignored-artifact fallthrough after a denied `.git/info/exclude` update, and noncanonical compact JSON sidecars; the route now emits an exact six-artifact contract, hard-gates repository-local writes on a proven ignore rule, and requires shipped-schema validation with nullable unloaded intent, with negative replays guarding the first two regressions.
2026-08-10 prompt-artifact controller — added an ignored-path-gated, schema-validated, stable-id, idempotent `artifacts goal-saved` command so full-plugin prompt runs no longer rely on the model to hand-author canonical Goal Binding and final-summary JSON.
2026-08-10 prompt-contract seal — host replay showed the model overwriting controller output and shortening the Goal contract after generation; the controller now cross-checks the exact single-line Goal, fallback, safety/proof/stop fields, seals controller-owned artifacts read-only, and must be the route's final write.
2026-08-10 route-load hardening — a fresh Claude replay followed the main six-file rule but skipped the prompt route module and controller command; the mandatory controller gate now lives in the always-loaded SKILL.md and fails closed if stable sealed sidecars are not returned.
2026-08-10 portable controller dogfood — a cross-repository Claude plugin run exposed a target-relative `<plugin-root>` placeholder; controller calls now use Claude's `${CLAUDE_PLUGIN_ROOT}` or another host's surfaced absolute skill root and explicitly forbid resolving the controller from the target repository.
2026-08-10 planning-only boundary dogfood — the portable-root rerun successfully produced exactly six ignored artifacts, consumed its request, emitted schema-valid canonical sidecars, and sealed the Goal/JSON read-only, but it also ran two repository proof probes before approval, created Python caches, and exhausted the $1 bound before reporting; prompt-to-Goal research is now explicitly static-only before Phase 7 and a sanitized negative replay guards zero repository commands.
2026-08-10 final-write correction — artifact timestamps proved the host wrote `08-final-summary.md` after the supposedly final controller call to add returned IDs; the controller now renders and seals that Markdown itself, verifies the exact Git base, rejects symlinked paths, and returns every final artifact path.
2026-08-10 prompt hardening verification — full preflight, 83 controller/contract tests, 16 artifact cases, nine replay cases, Claude plugin validation, worktree archive smoke, and whitespace/conflict checks pass locally on macOS; exact-commit archive and hosted three-OS checks remain for the pushed commit.
2026-08-10 R1 capability truthfulness — separated controller dependency availability from the unavailable production mission bridge, made unattended eligibility fail closed on that bridge, and corrected contradictory skill/plugin/docs execution claims; full check-all passes with 84 controller tests, 16 artifact cases, and nine replays.
2026-08-10 R2 operation schema — added duplicate-safe immutable intent and terminal receipt contracts covering host Goal, Git/command, and reconcile-required actions without raw output, environment, or credential fields; all 13 mission-schema tests and JSON fixture parses pass with no event-type replacement.
2026-08-10 R3 operation journal — added locked, atomic, immutable intent/result persistence with idempotent equality, binding-drift rejection, duplicate-safe reload, and pending/reconcile-required crash recovery without changing transition events or storage primitives; full check-all passes with 101 tests.
2026-08-10 R4 host protocol — added six closed host action requests and typed receipts bound to trusted operation, scope, authorization, and runtime identities; forged fields, raw host data, missing native Goal IDs, and ambiguous reconciliation are fail-closed, with full check-all passing 111 tests.
2026-08-10 R4 worktree receipt correction — required successful prepare-worktree receipts to carry a stable worktree id, path, branch id, and branch name so the future CLI never invents persisted mission identity; focused schema/protocol/adapter tests pass.
2026-08-10 R5.1 mission start — added local-only validated mission initialization with immutable contract copies, stable attempt identity, crash-safe planned-to-authorized recovery, idempotent retries, and a JSON CLI; mission-runner availability remains false and full check-all passes 117 tests.
2026-08-10 R5.2 next action — added deterministic one-action host requests whose exact authority/runtime/input hashes are journaled before return; lost responses now yield reconcile-required without replay, contracts are sealed read-only, and full check-all passes 119 tests.
2026-08-10 R5.3 record/resume — added typed receipt persistence before terminal operation results, validated state advancement, manual/reconcile fail-closed outcomes, and start/next/record/resume CLI recovery through a verified local branch; full check-all passes 123 tests while publication and mission-runner availability remain disabled.
2026-08-10 R6 worktree crash matrix — before-intent through after-transition fixtures preserve one worktree effect and require reconciliation after an ambiguous response.
2026-08-10 R6 Goal activation crash matrix — native Goal activation is never replayed after a lost response; stable identity or manual blocked handoff remains mandatory.
2026-08-10 R6 implementation crash matrix — persistent host fixtures prove intent-first execution, receipt/result recovery, and one implementation effect across restarts.
2026-08-10 R6 verification crash matrix — verification receipts recover across both journal boundaries without rerunning the verifier action.
2026-08-10 R6 commit crash matrix — one stable commit record survives receipt, result, and transition crashes; repeated receipts are idempotent.
2026-08-10 R6 push crash matrix — an ambiguous push stays pending/reconcile-required and the operation driver refuses a blind second push.
2026-08-10 R6 pull-request crash matrix — a lost create response reuses the exact persisted head/base/mission PR and creates at most one PR record.
2026-08-10 R6 check-poll crash matrix — ambiguous polls require reconciliation and ordinary pending checks remain bounded; full discovery and check-all pass 130 tests locally.
2026-08-10 R7 verified local bridge — `mission_runner_available` now truthfully reports the callable start/next/record/resume protocol while unattended eligibility remains false without host attestation; exact local flow and Goal-only/manual fallback are documented, the Codex dogfood stopped safely at typed manual handoff, Claude remains explicitly unverified/Goal-only, publication is disabled, and full preflight passes 130 tests, 16 artifact cases, and 10 replays.
2026-08-10 local budget enforcement — authorization can narrow but not widen Goal Binding limits; every typed action carries a restart-stable wall deadline, expiration persists `budget-limited` before a new action, late success is refused for reconciliation, and full preflight passes 135 tests while token/cost remains an explicit host non-guarantee.
2026-08-11 protected surface registry — added a versioned nine-category baseline, deterministic repository-relative classification, additive-only explicit policy, sealed per-mission policy hashes, and receipt-time undeclared-path rejection; full preflight passes 146 tests while truthful complete changed-file reporting remains a host non-guarantee.
2026-08-11 ShellCheck gate — added a single warning-or-higher lint command over all 15 Bash sources, an adversarial discovery/failure meta-test, local preflight and package-smoke enforcement, and Linux/macOS/Windows CI installation; full preflight and worktree archive smoke pass with 146 controller tests.
2026-08-11 J0.1 authority inventory — generated prompt Markdown was the only production Markdown-to-state input; legacy intent migration remains a deliberate Markdown reader, while eval harness reads are UX/safety assertions and JSON readers remain canonical. No second runtime owner was found.
2026-08-11 J0.2-J1.3 prompt JSON authority — added byte-identical golden renderers, one-line/untrusted-text identity guards, and tamper-repair coverage; `artifacts goal-saved` now validates the complete JSON bundle before writes, generates and seals both Markdown views without reading them, rejects the retired Goal-file CLI input, and the full preflight plus package archive pass with 155 controller tests and all recorded replays.
2026-08-11 J2.1 mission projections — added cross-contract identity/hash validation over mission state, Goal Binding, authorization, Runtime Boundary, protected policy, operation intents/results, and typed receipts; schema-v1 run logs cover active/terminal states, final summaries remain honestly terminal-only, and no argv/environment evidence is invented.
2026-08-11 J2.2-J2.3 mission views — added retry-safe `artifacts mission-view`, deterministic redacted run-log/final-summary renderers, active replacement and terminal sealing, tamper/symlink/ignore/crash tests, required autonomous checkpoint refresh instructions, and a no-host-action-replay repair fixture; full preflight and worktree archive smoke pass with 170 tests and all recorded replays.
2026-08-11 J3.1 exploration renderers — added versioned candidate and verification blocks generated only from schema-v1 JSON, strict malformed/nested/duplicate marker rejection, Markdown/HTML neutralization, byte-preserved narrative, golden output, JSON-change, CRLF, tamper-repair, and idempotency coverage.
2026-08-11 J3.2 eval authority — removed candidate identity, rejection, and downgrade-grade reads from authored verification Markdown; schema-valid JSON sidecars and the duplicate-safe bundle validator now supply those facts while funnel Markdown remains UX-only, all negative cases retain their named failure reasons, and full preflight passes with 179 tests.
2026-08-11 J3 Windows rendering correction — hosted Windows exposed replacement of the renderer's literal Unicode card separator before golden comparison; generated card structure now uses an ASCII separator while schema values remain Unicode-capable.
2026-08-11 J4.1 canonical intent store — added one lock-protected, duplicate-safe, schema-valid store for charter/roadmap/doctrine JSON plus deterministic replaceable Markdown views; all three documents validate before writes, JSON lands before views, symlinks fail closed, untrusted values are escaped, and tamper/crash tests preserve unchanged canonical hashes; full preflight passes with 187 tests and no schema changes or legacy deletions.
2026-08-11 J4.2 creator-confirmed intent activation — added `migrate intent-activate` requiring explicit confirmation and all three schema-valid JSON inputs before backup or writes; exact legacy CRLF bytes are backed up, partial JSON/view failures restore the original file set, symlinks and stale schemas fail closed, incomplete intent remains unresolved, reactivation repairs views without JSON drift, the legacy reader remains callable for one release, and full preflight passes with 195 tests while authorization/autonomy stay false.
2026-08-11 J4.3 canonical intent route cutover — creator refresh now activates schema-valid charter/roadmap/doctrine JSON together through the controller only after explicit confirmation, autonomous selection reads only canonical JSON while Markdown stays replaceable, legacy/view-only state is goal-only, controller-less installs degrade to conversation drafts, and focused consistency, behavior, eval, migration, template-schema, ShellCheck, and adversarial guard checks pass.
2026-08-11 J5.1 Markdown authority enforcement — added a function-scoped production guard allowing only legacy intent conversion and generated-region view replacement, plus seeded direct-reader and same-renderer-module adversarial fixtures; operator, artifact, and contributor docs now identify canonical JSON and the narrow reader policy, while full preflight passes with 195 tests and 16 ShellChecked Bash files.
2026-08-11 J5.2 exact-package dogfood — the isolated 283-file archive from implementation commit `8d318c05fc67bcd7526e85146b2ad4377c6fdd10` passed prompt Goal generation, prompt/intent view-tamper repair with unchanged JSON, seeded mission-view crash recovery with unchanged state, and legacy/incomplete intent goal-only checks; the legacy migration route remains because the controller CLI still calls it, no replay or product correction was needed, and full preflight, exact Git archive smoke, hosted Linux/macOS/Windows, CodeQL, and Dependency Review pass without live credentials, publication, merge, or independent native-host autonomy guarantees.
2026-08-11 malicious-filename fixture — added a cross-platform repository probe test whose option-like shell-substitution filename remains inert NUL-delimited Git status data while dirty-tree detection still succeeds; all six repository tests pass without production changes or shell execution.
2026-08-11 multi-surface injection replay — strengthened the blocked replay schema to require source, README/docs, test, diff, tool-output, intent-view, and prior-artifact evidence; the offline harness verifies an inert sentinel in every physical fixture while the replay contract requires zero repository commands and a blocked final state, with focused schema, all 10 replay, and ShellCheck gates passing.
2026-08-11 adaptive artifact budget — full exploration now records selected/skipped scout domains once, creates compact briefs only for selected domains, and treats expanded scout narrative as optional; intentionally skipped phases and future lifecycle states no longer receive placeholders, with a consistency guard preventing skipped-domain placeholder regressions.
2026-08-11 semantic progress checkpoints — added one always-loaded user-facing update contract for route, evidence, Goal, and execution transitions; updates use a compact changed/evidence/next shape, avoid per-file/internal narration, and remain explicitly separate from required per-controller-checkpoint mission-view persistence.
2026-08-11 canonical policy documentation — replaced the hand-copied protected-surface summary with a deterministic category/description/pattern table generated from `policies/protected-surfaces.v1.json`; duplicate JSON keys and malformed markers fail closed, and local, package, and three-OS preflights now reject documentation drift.
2026-08-11 crash-point coverage closure — audited the local and publication recovery matrices against the P2 checklist, added an explicit controller action-set guard so crash coverage cannot silently shrink, and recorded existing intent/side-effect/receipt/result/transition plus push/PR/check-poll evidence; focused recovery tests pass and publication remains disabled in the local mission bridge.
2026-08-11 GitHub API fixture matrix — added deterministic protected, unprotected, active-ruleset, and merge-conflict observations that all stop at awaiting-review with zero merge attempts; existing fixtures cover explicit auth/rate errors, bounded pending/failed checks, and exact PR reuse, while publication remains uncomposed and credential-free in the local bridge.
2026-08-11 monorepo intent namespaces — preserved the root `.pathfinder/` contract while adding isolated `.pathfinder/scopes/<scoped-root>/intent/` stores and creator-confirmed activation for explicit existing subprojects; normalized path, missing-directory, reserved-path, symlink, sibling/root fallback, Windows-separator, and interrupted-activation cases fail closed, with runtime routes and operator docs bound to the selected namespace.
2026-08-11 persisted Goal-pack queues — added explicit `run all` authorization over ordered canonical binding hashes, atomic one-active-item queue state, isolated one-Goal child missions, typed native `complete-goal` enforcement before advancement, restart/deadline/block/symlink/tamper recovery, full pack start/next/record/resume/status/abandon CLI coverage, and adversarial route guards; full preflight passes with 223 tests while publication, self-merge, and implicit queue growth remain disabled.
2026-08-11 worktree package fixture preservation — replaced basename-wide tar exclusions with a NUL-safe Git worktree file list so ignored runtime state remains excluded while tracked nested `.pathfinder` adversarial fixtures stay in the package; ShellCheck and the complete worktree archive smoke pass with 223 tests and all recorded replays.
2026-08-11 credential-free host install/load smoke — added an isolated exact-tree harness that installs and exposes the namespaced Codex plugin skill, separately discovers a repo-scoped `.agents/skills` copy, and strictly validates/installs/parses the Claude plugin inventory; full preflight and the worktree package archive pass with 223 tests on Codex CLI 0.146.0 and Claude Code 2.1.226, while model behavior, native Goal execution, and host autonomy remain explicit non-guarantees.
2026-08-11 K0.1 conditional self-merge contract — ratified a design-only two-key authority model, three credential boundaries, pinned GitHub REST `2026-03-10` plus complete REST/GraphQL evidence map, independent-human/check floors, bypass visibility, typed blockers, one-use crash reconciliation, and the residual control-plane race; publisher and enabled local transition maps retain zero merge calls, no schema/behavior/credential/publication authority changed, and full preflight passes with 223 tests.
2026-08-11 K1.1 merge authority schemas — added closed Draft 2020-12 host-policy and current-run authorization contracts with exact repository/policy/mission binding, canonical hashes, expiry, strict diff/review/check/single-PR floors, authenticated-source assertions, and adversarial paired fixtures; full preflight passes with 230 tests while existing mission authorization, controller routes, credentials, network behavior, and merge callers remain unchanged.
2026-08-11 K1.2 merge evidence/journal schemas — added closed v1 evidence, one-use intent, and terminal result contracts with complete pagination, exact policy/authorization/repository/PR/head/base/actor binding, typed unknown/unsupported states, canonical hashes, structured merged proof, and response-loss reconciliation fixtures; full preflight passes with 237 tests while existing operation enums, production callers, network behavior, credentials, and remote writers remain unchanged.
2026-08-11 K2.1 read-only GitHub merge observer — added a deterministic zero-network/zero-mutation evidence seam for repository/actor/PR/ref/file identity, merge settings/reconciliation, classic and inherited rulesets/bypass actors, reviews/requests/threads, checks/statuses/deployments, pagination, request ids, ETags, timestamps, and hashed unknown fields; typed fixtures fail closed for transport, 404 proof, ambiguity, malformed/future data, pagination, ruleset attribution, and bypass visibility, while merge queue, required deployments/signatures, code scanning/quality, file/metadata restrictions, and future rules remain unsupported and full preflight passes with 247 tests.
2026-08-11 K2.2 GET-only GitHub boundary with live limitation — added an uncomposed fixed-`api.github.com` TLS GET transport, evidence-only endpoint/query allowlist, read-only App credential declaration, pinned REST version/headers, byte/page/time/retry/redirect ceilings, duplicate-safe JSON, request-id/ETag audits, typed failures, and response/credential redaction; 11 fake-transport tests and full preflight pass with 258 tests, but declared scope is not positive token identity and GitHub's POST-only GraphQL review-thread/queue evidence remains unavailable, so live observation and merge stay unsupported with zero production callers or mutating methods.
2026-08-11 K3.1 pure conditional-merge eligibility — added an unused deterministic AND-only evaluator for closed/hash-bound authority, evidence completeness, repository/PR/diff/path floors, classic plus all active rulesets, source semantic hashes, squash allowance, bypass actors, permission-qualified independent-human reviews, GitHub review decision/threads, and exact unioned App/SHA/status checks; typed precedence is unknown over unsupported over policy-blocked over eligible, 35 focused and all 277 repository tests pass, and the K2 evidence contract now exposes allowed methods, source/aggregate rule hashes, review decision, and reviewer write/admin permission without adding any caller, credential, network path, mutation, or live merge support.
2026-08-11 PR review correction — bound single-Goal authorization to the Goal Binding's charter/roadmap/doctrine hashes with a focused regression test, reconciled the autonomous route, controller/threat contracts, master completion note, version notes, and OKF concepts with the enabled bridge's host-attested local-only boundary, and restored the protected `version + JSON consistency` check as an aggregate over the cross-platform preflight matrix; the host—not a command-free controller action—enforces structured argv, while GitHub publication and self-merge remain uncomposed.
2026-08-11 K3.2 freshness and drift reevaluation — capped complete evidence at the earlier of host expiry and 60 seconds from observed start, added a pure two-snapshot reread that rejects reused request identities and whole-domain repository/actor/PR/diff/rules/review/check drift, and covered the exact boundary plus base advance, force-push, ruleset update, review dismissal, check rerun, actor rotation, and policy-hash change; all 280 tests pass and no caller, cache, credential, intent, network, or merge path exists, while GitHub's lack of an atomic base/policy/protection/review/check precondition leaves trusted-admin mutation after the final reread as an explicit residual TOCTOU risk.
2026-08-12 K3.3 independent-review remediation — bound authorization to the exact controller publication/PR/refs/canonical diff, authenticated Git-object evidence, and a closed low-risk Goal classification; anchored protected surfaces to the shipped baseline with additive-only overrides; independently recomputed protected/special surfaces; added policy-controlled freshness and distinct host-policy reads; restricted approvals to attested humans excluding implementation/check actors; normalized and cross-checked classic/ruleset settings; and made single snapshots advisory while the inert journal persists and replays two full evidence documents and the exact protected-policy input into an immutable readiness proof. Malformed policy, ruleset code-owner, fabricated snapshot, stale intent, and weaker-baseline regressions now fail closed; three independent review axes are clean and all 287 repository tests plus the actual-code merge-evidence eval pass. No production caller, credential, route, network/mutation primitive, or merge capability was added; K4 remains closed pending durable PR identity, live complete evidence, an authenticated host-owned proof envelope, and separate enablement authority.
2026-08-12 K3 acceptance-matrix closure — reconciled the conditional-merge checklist with actual typed behavior, made ruleset bypass mode mandatory and eval-covered, distinguished exact actor/App bypass matches from unresolved team/role/admin membership, added source/parent failure and enforcement-drift proofs plus direct regressions for every protected/special surface and retarget/diff drift, and linked 16 K3 observer/evaluator/inert-composition cases to executable evidence. Positive membership resolution remains an explicit K3 follow-up, while the two merge-writer response-loss/reconciliation cases remain deliberately unchecked and K4-only. Full preflight, all 290 tests, the actual-code eval, worktree package smoke, and credential-free Codex/Claude install/load smoke pass; no caller, credential, network primitive, mutation, or merge capability was added.
2026-08-12 K3 membership-resolution closure — added a closed exact-coverage evidence family for classic/ruleset bypass membership, bound every fact to source-projected team/role metadata, the exact bot identity, one qualified exact-endpoint audit, and the policy source, and included the full membership domain in two-snapshot drift comparison. Team/repository-role/organization-admin match/no-match semantics are independently derived in the observer/evaluator; GitHub's idless organization-admin/deploy-key representation is preserved; missing/shared/unqualified audits and missing/duplicate/pending/extra/drifted facts fail closed. The GET-only allowlist admits only the exact read-only membership endpoints and requires organization Members read; all 17 K3 acceptance cases and the actual-code eval are owned by deterministic fixtures. Full preflight passes all 297 tests, and the exact worktree archive plus credential-free Codex/Claude install/load smoke passes. No live backend composition, credential, production caller, mutation, writer, or K4 authority was added.
2026-08-12 K4 crash-safe merge primitive — added an unreachable/default-off executor, separate write-once merge journal, one-repository GitHub App credential policy, fixed-host synchronous squash backend, closed terminal reason/proof contract, and explicit reconciliation-only recovery. Fixtures prove one exact SHA-bound PUT at most, four exact follow-up GETs, typed 401/403/404/405/409/422/rate/malformed/server outcomes including authenticated already-merged classification, exact repository/PR/head/base-parent/actor/time proof, and crashes before intent through after terminal persistence without a second PUT. Review remediation atomically spends authorization/readiness across operation ids, binds a fresh host-authenticated credential receipt, and restricts a combined lock-held dispatch/send boundary to the fresh intent creator's process-local capability; the backend persists dispatch only after deterministic request preparation at its final pre-transport boundary. Concurrent/forked sends, merged-without-dispatch, and credit for known preparation/pre-dispatch crashes are rejected; because a local marker cannot prove a remote request began, restarted pending dispatches remain reconciliation-required even when observation finds matching merged state. The strengthened intent/result artifacts are v2 because the K1 v1 preview was never composed; deterministic offline evals exercise the real writer and schemas, and v1 rejection is tested. The full preflight passes 334 tests. Repository search proves zero executor callers: CLI, mission host, Goal packs, ordinary publication, and installed skill routes remain awaiting-review-only and load no merge credential; K5 composition remains closed.
2026-08-12 exact publication prerequisite — added an uncomposed authenticated request/dispatch/receipt journal plus explicit injected awaiting-review controller. It atomically spends one mission authorization/candidate, persists exact repository and PR database/node/number/ref/SHA/diff identity, replays terminal receipts without network, and recovers lost create responses only through read-only exact lookup/check observation with zero second push or create. Concurrent dispatch is single-send, re-hashed out-of-window dispatches fail closed, and a crash after the durable dispatch marker but before any remote call recovers through observation only. The actual-code artifact eval, full 354-test preflight, and exact archive plus credential-free Codex/Claude install smoke pass; no CLI, mission host, Goal pack, installed route, live backend, credential loader, or merge path constructs it. K5.1 and K5.2 remain closed pending independent review and remaining trusted-host/live-rehearsal gates.
2026-08-12 publication prerequisite review remediation — embedded and canonically bound the full explicit GitHub-awaiting-review authorization and one-PR ceiling; removed caller-selected time so only the injected host clock can prove freshness; added read-only exact repository/ref/commit/diff/check preflight before mutation; persisted every successful check's context, App id, and head SHA; and released the journal lock before the remote callback so real process death cannot strand observation-only recovery. Direct adapter/controller/schema/journal regressions cover backdated expiry, wrong preflight identity, wrong check context/App/SHA/state, authorization drift, lock-free callbacks, and an actual forked process death followed by receipt recovery. The actual-code eval asserts every preflight field, both independent source reviews are clean, and the full preflight passes 365 tests. K5 remains closed because no enabled caller, live backend, credential loader, or installed route was added.
2026-08-12 K5 readiness truthfulness — reconciled the checked source-review prerequisites with the still-missing runnable publication, installed trusted host, complete live evidence, operator-owned policy/credential boundaries, zero-merge publication/evidence rehearsal, K5.1 review, and K5.2 human approval; the later composed merge rehearsal remains a distinct K6 gate. A package-wide regression now scans Python, scripts, skills, `.agents`, and both plugin surfaces for publication/merge constructors, an exact live backend, merge credential imports, or publish/merge CLI commands. Focused contracts and the full 365-test preflight pass with no new runtime capability; K5 remains closed.
2026-08-12 direct-publication bypass guard — closed the default-off regression gap that checked the publication controller but not direct construction of its lower-level GitHub publisher. The package-wide contract now rejects every publisher/controller/executor constructor plus concrete generic or exact GitHub backends outside the source-only protocol owner; coverage and operator docs name this boundary. Focused contracts pass with zero callers and no runtime capability; K5 remains closed.
2026-08-12 bounded zero-merge live rehearsal — created separate repository-scoped publication and read-only observation GitHub Apps outside repository trust, ran the real source publication controller once against a private disposable repository, persisted and idempotently replayed one exact awaiting-review PR receipt, and collected two complete 16-request REST/GraphQL evidence snapshots with matching diff hashes, disjoint request ids, no unknown/unsupported fields, and no security-domain drift. The private Free-plan target proved protection/rules features unavailable and remains policy-blocked; PR 1 is open/unmerged with zero deployments/releases and no merge App, credential, intent, or request. The external adapter is not a shipped/installed host route and used a non-authorizing dry-run binding, so runnable installed publication, a schema-valid operator merge policy, K5.1, and K5.2 remain unchecked.
2026-08-12 K5.1 observation-only merge status — added explicit `merge status` and `merge evaluate` commands that lazily read one exact persisted awaiting-review publication receipt plus fixed operator-owned policy and two-snapshot evidence files from a current-user-owned, owner-only non-symlink host directory outside repository trust. Both snapshots and authorization are independently rebound to the receipt before the pure evaluator emits a closed hashed JSON report or Markdown view; missing/malformed inputs fail closed, and a missing receipt stops. State remains `awaiting-review`, execution/writer/intent flags are schema-fixed false, readiness proofs are discarded, and package scans forbid writer, credential, network, environment-token, intent, or dispatch primitives from the sole read-only caller. The full preflight and worktree archive smoke pass with 376 tests. K5.1 still awaits independent review; K5.2 remains closed and unimplemented.
2026-08-12 K5.1 independent-review closure — pinned every installed-host read to one verified POSIX directory descriptor, failed Windows closed pending equivalent ACL ownership proof, bound the selected journal filename to its embedded request id, canonically correlated every present or malformed JSON input in the report, closed input-state and deny-code domains, and added an actual-code artifact eval. Direct regressions cover path swaps, symlinks, renamed records, non-object and null JSON, evidence identity changes, and tampered report bindings. Independent standards and adversarial security/spec reviews are clean; the complete 382-test preflight and exact package archive pass. K5.2 remains closed and unimplemented pending separate human security approval.
2026-08-12 fixed-query GraphQL evidence boundary — added an uncomposed fixed-`api.github.com/graphql` TLS client whose transport can POST only the compiled `PathfinderPullRequestEvidence` query and accepts no operation text or URL. It hash-binds exact PR/repository/ref/mergeability/review-decision/queue facts and independently paginates latest reviews, code-owner requests, and review threads with stable-identity, total-count, cursor, item, request-id, byte/page, enum/field, and partial-error fail-closed checks. Twelve fake-transport tests plus the existing REST/observer suites pass; the REST process remains GET-only, no source loads secrets, and no production caller exists. This is not the complete live collector or an installed host route, so positive App/installation/bot identity, qualified protection/rules absence, operator credential injection, both K5 installed-host readiness items, and K5.2 remain open.
2026-08-12 evidence identity and qualified-absence boundary — added a closed hash/current/repository-bound host receipt for the exact read-only observer token, an uncalled verifier that independently cross-checks observer and future merge App/installation/bot identities plus the repository, and a narrow feature reader that accepts private-plan protection/rules absence only with the exact endpoint, required-permission response header, and closed GitHub upgrade response. Ordinary 403 still fails; live permission, suspension, repository, receipt hash/time, and actor drift have direct regressions. No secret loader, command, collector caller, publication/merge route, or mutation was added. Complete REST/GraphQL normalization, membership/check-suite coverage, last-pusher proof, installed host injection, and K5.2 remain open.
2026-08-12 Windows hosted-preflight PATH hardening — exported Chocolatey's tool directory through GitHub Actions' cross-step PATH after installing jq and ShellCheck, and added a portability regression guard for every workflow that installs ShellCheck through Chocolatey. The validation matrix remains unchanged and no check is skipped or weakened.
2026-08-12 membership and complete-check read boundary — added an uncalled resolver for exact team, organization-admin, and repository-role bypass membership with endpoint-specific Members/Metadata read qualification, unique request audits, closed source metadata, exact merge-bot binding, and explicit maintain/triage representation. Check collection now lists every exact-SHA suite and paginates each suite's runs under one global ceiling, rejecting the documented 1,000-suite shortcut, duplicates, reused request ids, and suite/SHA drift. Focused adversarial tests pass; no credential loader, installed collector, command, publication/merge caller, or mutation was added, and rules/reviewer/check classification plus controller last-pusher composition remain open.
2026-08-12 permission-qualified REST review boundary — added an uncalled exact-PR review reader that fully paginates the chronological REST audit, closes review/actor/state/SHA/time/association fields, reads one current repository permission per unique actor, requires positive Metadata-read response evidence, cross-checks returned id/login, preserves target-bound audits, and rejects duplicates, identity drift, incomplete pages, unknown fields, reused request ids, and global request-budget overflow. All 410 controller tests pass; no credential loader, installed collector, command, publication/merge caller, or mutation was added, while REST/GraphQL reconciliation, policy/rules/check classification, controller last-pusher binding, and K5.2 remain closed.
2026-08-12 exact check-evidence projection — extended the uncalled suite-first reader to normalize exact-SHA check runs, read the combined status envelope, fully paginate the creator-bearing status history, derive the latest item per context, and cross-check its count/state under one shared 30-request ceiling. Run App/context/terminal state and status creator/context/time are closed; required flags derive only from a closed supplied context/App union; every required run must carry the supplied exact PR/head/base identity; and duplicate current run/status/request identities, unknown fields, incomplete pages, or repository/PR/SHA/state drift fail closed. All 414 controller tests pass; policy/rules and PR sources do not yet supply those inputs, complete snapshot composition remains open, and no credential loader, command, installed collector, mutation, or K5.2 path was added.
2026-08-12 required-check policy union — added a pure, uncalled projector that unions the host policy floor with a permission-qualified classic-protection response and every item in the complete active-rules page set. Exact context/App identities are sorted and deduplicated across sources, while empty host floors, unpinned/null/any-App requirements, classic context/check disagreement, malformed source attribution, duplicate rule identities, unknown check fields, and incomplete or reused-request pagination fail closed. All 419 controller tests pass; live policy/rules reads, PR input binding, collector composition, installed credential injection, mutation, and K5.2 remain closed.
2026-08-12 REST/GraphQL review reconciliation — preserved REST and actor node ids through the permission-qualified review reader, then added a pure uncalled reconciler that derives the latest opinionated REST review per actor and requires GraphQL's complete latest-per-user view to match exact database/node ids, actor identity/type, state, head commit, timestamp, and association. Missing/extra actors, comments laundering an older opinion, malformed permissions, identity drift, nonchronological REST history, incomplete pagination, duplicate identities, or cross-protocol request-id reuse fail closed. All 424 controller tests pass; complete review-request/thread and snapshot composition, installed credentials, mutation, and K5.2 remain closed.
2026-08-12 authenticated publication-pusher binding — bound the publication bot's exact database id, node id, and login through the canonical host request, pre-mutation exact target, controller receipt, and journal request binding. A pure uncalled reconciler independently validates both document schemas/hashes and mission/diff/check/actor bindings, then requires a later fixed-query GraphQL view of the identical repository, PR database/node/number, head/base repositories, refs, and SHAs before projecting the controller pusher id. Rehashed actor/mission/diff/check/URL drift, malformed targets, stale or ambiguous GraphQL audits, query drift, and object/ref/SHA drift fail closed. Full preflight passes all 432 tests; no credential loader, network caller, installed collector, mutation, or merge route was added. Host-authenticated storage, exclusive controller branch ownership through the evidence instant, complete snapshot composition, and K5.2 remain closed.
2026-08-12 GraphQL provenance and review projection — pinned merge evidence to the compiled `PathfinderPullRequestEvidence` query hash and one actual `graphql-pull-request` request family, replacing synthetic per-connection provenance. A pure uncalled projector now requires the exact publication-pusher proof, identical repository/PR/head/base identity, complete latest-review/request/thread connections, unique closed reviewer/thread identities, and one-to-one ordered request/rate-limit audits before emitting normalized mergeability, queue, pagination, review-request/thread, and shared audit inputs. Missing GraphQL provenance now blocks even when fabricated per-surface audits remain; focused regressions also cover query, identity, pagination, duplicate, rate-limit, and audit-time drift. A Windows-hosted failure on filesystem-dependent structural-scan ordering was reproduced and fixed by sorting paths before comparison. Full preflight passes all 439 tests; there is still no credential loader, network caller, installed collector, mutation, complete snapshot composer, or K5.2 path.
2026-08-12 complete evidence composition boundary — added one pure, uncalled composer that requires the verified one-repository observer receipt and all four identity audits, authenticated publication request/receipt and controller pusher, schema-pinned GraphQL projection, reconciled permission-qualified REST reviews, host/classic/ruleset required-check union, exact check/status pages, and every remaining normalized REST family. It emits schema-valid evidence plus a separate canonical provenance receipt bound to the evidence, credential/publication receipts, query, reviews, checks, request ids, and collection window; split identities and cross-surface request reuse fail closed. The composer owns no client, credential, host storage, command, installed caller, network method, publication, or mutation. Positive credential/actor/bypass recording is now closed, while durable host authentication, exclusive controller-branch ownership, both installed-host readiness gates, and K5.2 remain open.
2026-08-12 controller-branch ownership proof — added a pure, uncalled proof that binds the reconciled controller pusher to one closed publication-credential receipt, an active repository ruleset dedicated to restricted branch creation/update/deletion, exactly the publication App as sole always-bypass actor, the complete effective rules for the exact controller branch, and a final qualified ref/SHA reread after evidence completion. The complete composer now requires the canonical proof, binds it into provenance, and rejects cross-surface request reuse or publication/repository/ref/actor drift. Focused negatives cover missing rules, extra bypass actors, fetch-and-merge, malformed credentials, stale/incomplete/unqualified reads, request reuse, and ref drift; the full preflight passes 450 tests. No client, token, storage, command, installed collector, publication/merge caller, or mutation was added; both installed-host readiness gates and K5.2 remain closed.
2026-08-13 authenticated host-artifact envelope — added a source-only, zero-caller immutable store whose injected external authenticator covers one atomic envelope containing the exact publication journal, publication/observer credential receipts, controller-branch ownership proof, evidence, and provenance. It independently validates their canonical hashes and complete repository/mission/PR/ref/SHA/diff/credential/review/check/time bindings; POSIX roots and records are descriptor-pinned, owner-only, non-symlink, outside repository trust, size-bounded, write-once, fsynced, idempotent, and concurrent-safe, while Windows fails closed. Re-hashed tampering, wrong-store/renamed replay, split identities, stale ordering, interrupted writes, path overlap, and replacement all have direct regressions and an actual-code eval. The integration also closed a composer seam that allowed publication and evidence diff/mission drift. All 460 controller tests and artifact evals pass; no authenticator implementation/key loader, client, token, command, installed collector, publication/merge caller, or mutation route was added, so the two installed-host gates and K5.2 remain closed.
2026-08-13 authenticated authority pairing — upgraded the source-only host-artifact envelope to v2 so one external attestation also covers the exact operator policy, current-run authorization, and shipped-baseline or additive protected policy. The store now reproduces policy/authorization/evidence/candidate/time bindings and the effective additive protected-policy hash. Its paired K5.1 consumer loads two explicit, distinct evidence ids, re-verifies both envelopes, and rejects publication, authority, or authenticator/key drift before pure evaluation. The full 463-test preflight, actual-code artifact eval, worktree archive, and credential-free Codex/Claude host-install smoke pass; no authenticator/key implementation, constructor route, credential loader, client, network call, publication/merge caller, or mutation was added. Both installed-host readiness gates and K5.2 remain closed.
2026-08-13 source-only authenticated evidence collector — composed the fixed observer identity, GraphQL, permission-qualified review, required-check, and exact check/status readers behind one unconstructed boundary that requires a single shared observer installation credential and no merge credential. It eagerly snapshots every injected normalized base surface, uses only a trusted injected clock, closes evidence before the post-window branch-ownership proof, emits canonical evidence/provenance, and persists the exact externally attested envelope; a real-store reload and stale/mixed/backwards/lazy-read negatives cover the boundary. The full 477-test preflight and artifact evals pass. The remaining normalized REST policy/diff/deployment backend, ownership reader, authenticator/key, credential injection, and installed command/route are still external and unshipped, so neither installed-host gate nor K5.2 is checked.
2026-08-13 candidate and branch-ownership REST closure — added fixed source-only readers for the exact authenticated PR, both refs, fully paginated changed files, exact-head deployments, a second merge-state PR view, configured ownership ruleset, complete effective branch rules, and final same-SHA ref. Candidate identity/ref drift and omitted non-binary patches fail closed; binary omissions use the controller object receipt rather than inference. Ownership requests require positive Metadata/Contents permission headers, visible sole publication-App bypass, ordered unique audits, and the same observer credential as every collector reader. The collector now derives the publication pusher before these reads and accepts only the remaining normalized classic/ruleset/membership backend as an injected REST projection. The full preflight passes all 487 tests. No secret loader, authenticator/key, installed constructor/command, publication mutation, merge credential, intent, request, or K5.2 route was added; both installed-host gates remain unchecked.
2026-08-13 normalized policy REST closure — added one unconstructed GET-only reader that permission-qualifies and fully paginates classic protection, aggregate active rules, the parent-inclusive source index, each referenced ruleset detail, derived bypass actors, and exact supported membership resolutions. The collector now consumes one immutable policy snapshot for both normalized evidence and required-check projection, eliminating mixed-read inputs. Source/detail/parameter/request drift, private-plan ambiguity, hidden bypass actors, unknown fields, unsupported source identity, and request ceilings fail closed; ruleset Team/RepositoryRole actors remain deliberately ineligible without source-owned names. The complete preflight passes all 497 controller tests and every artifact/replay/validator/portability check. No secret/authenticator/key loader, installed constructor/command, publication mutation, merge credential, intent, request, or K5.2 route was added; exact merge-actor verification and both installed-host gates remain open.
